{
  "name": "fs-glide-path",
  "version": "1.0.0",
  "description": "flight simulator glide path calculator",
  "main": "index.js",
  "scripts": {
    "build": "copyfiles -f -V src/* dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
